#include "main.h"

/**
 * _abs - returns the absolute value of a number
 * @n: - number to be checked
 * Return: Absolute value of n
*/
int _abs(int n)
{
if (n < 0)
return (n * -1);
else
return (n);
}
